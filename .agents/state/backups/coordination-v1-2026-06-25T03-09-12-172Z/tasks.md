# Project Task Ledger

> 项目级任务账本模板。它是当前项目的执行任务源；全局 dashboard 只能聚合状态，不应替代项目 ledger。

## 推荐状态

- `ready`：可领取
- `running`：已领取
- `review`：已提 PR，等待审查
- `blocked`：被外部条件阻塞
- `done`：已完成

## 任务格式

```md
| task_id | provider | repo | source_url | title | priority | risk | status | owner | model | branch | change_request_url |
|---------|----------|------|------------|-------|----------|------|--------|-------|-------|--------|--------------------|
| 001 | local | owner/repo | - | 修复登录回调 | high | low | ready | AI | gpt-5.3-codex | feat/login-fix | - |
```

## 单条任务建议包含

- Provider 和原始任务链接
- 目标
- 非目标
- 验收标准
- 相关 skill 和代码规范
- 影响范围
- 风险和回滚
- 验证计划
- 参考链接 / issue / PR / MR
- parent task / 原任务引用
- source / 原 PR/MR 引用
- reason / 为什么派生这个任务

## 自动化规则

1. 20 分钟执行器每次只领取 1 个 `ready` 任务
2. 领取前必须先形成 Task Contract
3. 领取前必须识别相关 skill、项目代码规范和测试约定
4. 领取后先建分支或 worktree，再改代码
5. 完成后必须附带测试结果和 PR/MR 链接
6. 审查器只处理 `review` 任务对应的 PR/MR，并按 Task Contract 逐条审查
7. 审查不合格优先退回原 PR/MR，只有原 PR/MR 无法继续或问题已合并才创建 follow-up 修复任务

## Active Tasks

| task_id | provider | repo | source_url | title | priority | risk | status | owner | model | branch | change_request_url |
|---------|----------|------|------------|-------|----------|------|--------|-------|-------|--------|--------------------|
| NR-001 | local | zuohuadong/elygate | - | 审计 new-api/Redis 使用点并形成迁移清单 | high | low | done | AI | gpt-5.3-codex | codex/noredis-audit | - |
| NR-002 | local | zuohuadong/elygate | NR-001 | 使用 @postgresx/noredis 迁移缓存/KV/TTL 场景 | high | medium | done | AI | gpt-5.3-codex | codex/noredis-cache | - |
| NR-003 | local | zuohuadong/elygate | NR-001 | 迁移限流、Pub/Sub 失效通知和分布式锁 | high | medium | done | AI | gpt-5.3-codex | codex/noredis-runtime-primitives | - |
| NR-004 | local | zuohuadong/elygate | NR-001 | 用 pg-boss/@postgresx/noredis 队列边界替代 Redis 队列场景 | high | medium | done | AI | gpt-5.3-codex | codex/noredis-queues | - |
| NR-005 | local | zuohuadong/elygate | NR-002/NR-003/NR-004 | 补齐 Redis-free 验证、配置文档和回滚路径 | medium | low | done | AI | gpt-5.3-codex | codex/noredis-verification | - |
| AM-001 | local | zuohuadong/elygate | user-request | 内置 Postgres-native Elygate Memory MVP | high | medium | done | Codex | gpt-5.3-codex | current-worktree | - |
| AM-002 | local | zuohuadong/elygate | user-request | 完整实施 Elygate Memory 运营能力 | high | medium | done | Codex | gpt-5.3-codex | current-worktree | - |
| EAG-001 | local | zuohuadong/elygate | user-request | 实施三层 monorepo 企业级 AI 网关架构 | high | high | done | Codex | gpt-5.3-codex | codex/elygate-enterprise-monorepo | - |
| EDV-001 | local | zuohuadong/elygate | user-continue | 企业级三层部署验证与回滚 Runbook | high | low | done | Codex | gpt-5.3-codex | codex/elygate-enterprise-monorepo | - |
| PX-001 | local | zuohuadong/elygate | user-request | 引入 @postgresx/noredis 发布版旁路验证 | medium | low | blocked | Codex | gpt-5.3-codex | codex/elygate-enterprise-monorepo | - |
| ARC-001 | local | zuohuadong/elygate | user-request | 裁决实施企业控制面资源视图模块化 | high | medium | done | Codex | gpt-5.3-codex | codex/elygate-enterprise-monorepo | - |
| ARC-002 | local | zuohuadong/elygate | user-request | 完成剩余架构优化并经 glm-5.2 审查后提交推送 | high | medium | done | Codex | glm-5.2 review + gpt-5.3-codex impl | codex/elygate-enterprise-monorepo | - |

### ARC-002 Task Contract: 完成剩余架构优化并经 glm-5.2 审查后提交推送

- 目标：完成上一轮架构审查剩余可落地项：拆分 gateway 启动组合点；让企业资源视图明确并执行可用的 gateway-instance projection scope；抽出通用 Panel 与 Enterprise Console 的 svadmin provider 复用逻辑；把 postgresx pilot 的后续替换 gate 固化为可追踪决策文档；使用 `glm-5.2` 做独立审查，达成一致后修复、验证、提交并 push 当前分支。
- 非目标：不部署生产；不执行生产迁移；不把 Enterprise Console 合并回通用 Panel；不扩大 `@postgresx/noredis` 到鉴权/限流/计费/缓存热路径；不改变公开 API 路由或 SupAuth scope 名称；不强推。
- 验收标准：`apps/gateway/src/index.ts` 启动职责被拆成可读 helper；企业资源视图不再无条件读全局 logs/tokens/memory，至少按已存在的 `external_workspace_id` / `org_id` projection 执行过滤并在响应标注 scope；Panel/Enterprise svadmin data provider 解析与 virtual-resource 包装共享 helper；postgresx pilot 文档写明 benchmark/feature-flag gate；`glm-5.2` reviewer 无 blocking findings 或已修复；`bun run check`、必要 Svelte autofixer、`git diff --check` 通过；提交并 push 到 `origin/codex/elygate-enterprise-monorepo`。
- 相关 skill 和代码规范：`agent-team-delegation-gate`、`typescript`、`elysiajs`、`database-profile-selector`、`postgres-database`、`svelte-code-writer`、`svelte-core-bestpractices`、`svadmin-admin-ui`；遵循三层 monorepo 边界、Bun/Elysia/Drizzle/Svelte 5/svadmin 现有模式。
- database_profile：target=postgres；decision_source=detected+architecture-review；data_scope=enterprise governance projection over existing core tables；consistency=read-only filters for this task；migration_strategy=无 schema migration；backup_restore=无数据破坏；verification=`bun --cwd apps/gateway test:enterprise`、`bun run smoke:enterprise:runtime`（如时间允许）、`bun run check`。
- 影响范围：`apps/gateway/src/index.ts`、`apps/gateway/src/enterprise/resourceViews.ts`、`apps/admin/src/App.svelte`、`apps/enterprise-console/src/App.svelte`、共享 provider helper、文档、`tasks.md`、`progress.md`。
- 风险和回滚：启动拆分可能改变 Elysia route/hook 顺序；资源过滤可能缩小企业视图范围；svadmin helper 可能影响列表解析。回滚可恢复原 inline 代码和全局资源视图；验证以 enterprise router tests、Svelte checks、runtime smoke 和根级 check 降低风险。
- Delegation Decision：risk=medium，多文件但以边界/重构/只读过滤为主；主进程串行实施，之后强制 `glm-5.2` 独立 review；若 reviewer 有 blocking findings，先修复再提交。
- interruption_recovery：两次 `agent-team subagent dispatch verifier --model glm-5.2` 均因 codex runtime `spawn codex ETIMEDOUT` 未产出可用裁决；改用直接 `codex exec -m glm-5.2 -C /Volumes/Data/workspace/elygate --sandbox read-only --ephemeral` 完成只读审查，session=`019ef526-5c4e-7062-9ade-aa980851c2a7`。
- glm-5.2 review：verdict=`PASS`，blocking_findings=[]；非阻断建议包括修复 `resourceViews.ts` 在非数字 SupAuth `org_id` 下静默退化为无 scope 查询、补充企业运行时守卫 fail-closed 503 的发布前迁移/预检说明、复核 `PORT` 行为变更和基础模式企业健康路由暴露。主进程已修复前两项；后两项为已记录的低风险行为/运维注意事项。
- 结果：启动职责已拆为 helper；企业资源响应返回 `scope_boundary`，org-scoped 视图不再在 `org_id` 不可投影时无约束读全表，request logs 保留 workspace projection；Panel 与 Enterprise Console 复用 `@elygate/svadmin-helpers`；ADR 和企业部署 runbook 固化三层边界、projection 语义、postgresx pilot gates 与企业模式 fail-closed 启动要求。验证：`npx @sveltejs/mcp svelte-autofixer apps/admin/src/App.svelte --svelte-version 5` 通过；`npx @sveltejs/mcp svelte-autofixer apps/enterprise-console/src/App.svelte --svelte-version 5` 通过；`bun --cwd packages/svadmin-helpers check` 通过；`bun --cwd packages/enterprise-contracts check` 通过；`bun --cwd apps/gateway check` 通过；`bun --cwd apps/gateway test:enterprise` 通过（12 tests / 123 expects）；`bun --cwd apps/admin check` 通过；`bun --cwd apps/enterprise-console check` 通过；`bun run check:layers` 通过；`bun run check` 通过；orchestrator verdict=`PASS`。

### ARC-001 Task Contract: 裁决实施企业控制面资源视图模块化

- 目标：落实架构审查裁决中的第一优先项，在不改变 `/api/enterprise/*` 外部行为、不修改数据库 schema、不触碰生产环境的前提下，降低 `apps/gateway/src/enterprise/controlPlane.ts` 的职责密度；先把 provider channels、model routes、gateway API keys、request logs、agent memories 五类只读资源治理视图拆到独立企业层模块。
- 非目标：不合并通用 Panel 与 Enterprise Console；不扩大 `@postgresx/noredis` pilot 到热路径；不变更 SupAuth scope、路由 URL、响应字段、数据库表或生产部署配置；不做完整 DTO/schema 大迁移。
- 验收标准：新增 `apps/gateway/src/enterprise/resourceViews.ts` 承接五类资源视图查询；`controlPlane.ts` 继续通过 `postgresEnterpriseControlPlane` 暴露同名方法；层边界脚本允许该企业组合文件且继续禁止基础 gateway/admin 直接依赖企业包；`bun run check` 通过；独立 verifier 或中断恢复证据落入 `.mailbox/`。
- 相关 skill 和代码规范：`agent-team-delegation-gate`、`typescript`、`elysiajs`、`database-profile-selector`、`postgres-database`；遵循现有 Bun/Elysia/Drizzle/TypeScript 严格模式和三层 monorepo 边界。
- database_profile：target=postgres；decision_source=detected+architecture-review；evidence=`packages/db/src/schema.ts`、`apps/gateway/src/enterprise/controlPlane.ts`、`apps/gateway/src/enterprise/resourceViews.ts`；data_scope=enterprise governance view over existing core tables；consistency=read-only resource views in this task；access_pattern=server-only/direct-tcp through existing Drizzle db；migration_strategy=无 schema migration；backup_restore=无数据写入或破坏性变更；verification=`bun --cwd apps/gateway check`、`bun --cwd apps/gateway test:enterprise`、`bun run check:layers`、`bun run check`。
- 影响范围：`apps/gateway/src/enterprise/controlPlane.ts`、`apps/gateway/src/enterprise/resourceViews.ts`、`scripts/check-layer-boundaries.ts`、`tasks.md`、`progress.md`。
- 风险和回滚：风险是拆分过程中遗漏方法、破坏企业资源视图响应或绕开层边界；通过保持函数签名和导出名不变、运行企业路由测试和根级 check 降低风险。回滚可删除 `resourceViews.ts`，把五类查询函数移回 `controlPlane.ts`，并移除边界脚本 allowList 项。
- Delegation Decision：risk=medium，多文件架构重构但不改运行时行为、不改路由、不改 schema；采用 explorer -> orchestrator implementation -> verifier。Explorer 首次因 codex runtime `ETIMEDOUT` 失败，主进程按现有代码证据实施；Verifier 首次 `ETIMEDOUT`，第二次缩小为静态核对后 PASS。
- interruption_recovery：explorer mailbox=`.mailbox/ARC-001-control-plane-explorer.md`，verifier mailbox=`.mailbox/ARC-001-control-plane-verifier.md` 均为 codex runtime `spawn codex ETIMEDOUT`；rerun verifier mailbox=`.mailbox/ARC-001-control-plane-verifier-rerun.md` 裁决 `PASS`，确认五类资源方法位于 `resourceViews.ts`、`controlPlane.ts` 继续导入并纳入 `postgresEnterpriseControlPlane`、`bun run check:layers` 通过。
- 结果：新增 `resourceViews.ts` 并从 `controlPlane.ts` 抽离五类资源治理视图；`controlPlane.ts` 从 1394 行降至 1185 行；`scripts/check-layer-boundaries.ts` 将 `resourceViews.ts` 归入企业组合 allowList。验证：`bun --cwd apps/gateway check` 通过；`bun --cwd apps/gateway test:enterprise` 通过（12 tests / 123 expects）；`bun run check:layers` 通过；`bun run check` 通过；`git diff --check` 通过；orchestrator verdict=`PASS`。

### EDV-001 Task Contract: 企业级三层部署验证与回滚 Runbook

- 目标：在不触碰生产环境的前提下，为 EAG-001 的三层 Enterprise 架构补齐发布前部署验证 runbook、企业环境变量模板和部署文档入口，明确 SupaCloud + SupAuth + svadmin 企业模式的健康检查、控制面 smoke、数据面 smoke、生命周期验证和回滚步骤。
- 非目标：不 SSH 到生产服务器；不 push；不重启服务；不执行生产数据库迁移；不泄露或写入任何真实密钥、token、数据库 URL、服务器 IP 或 admin password。
- 验收标准：新增 `docs/ENTERPRISE_DEPLOYMENT_RUNBOOK.md`；`.env.example` 包含企业模式所需的 `ELYGATE_*`、`SUPAUTH_*`、`ENTERPRISE_AUTH_MODE` 和 `PG_BOSS_*` 模板；`DEPLOYMENT.md` / `DEPLOYMENT_ZH.md` 指向新的 runbook；文档明确 Basic Gateway + Panel 可在禁用企业变量时继续运行；文档包含生产前本地 gate、生产 smoke、SupaCloud lifecycle 验证和回滚路径。
- 相关 skill 和代码规范：`agent-team-delegation-gate`、`deployment-target-selector`、`supacloud-platform`、`svadmin-admin-ui`；遵循项目中文协作规范，文档中仅使用占位符。
- deployment_profile：target=supacloud-or-existing-private-infra；decision_source=user-continue + EAG-001 docs；runtime_kind=backend + static admin UI + enterprise control plane；target_region=private-infra/unknown；data_residency=user-data/sensitive；domain_dns_owner=supacloud-caddy 或 existing；stateful_services=postgres、pg-boss、supauth、supacloud app lifecycle；secrets_strategy=仅占位符，不打印真实 secret；verification=`git diff --check`、`bun run check:layers`。
- 影响范围：`.env.example`、`docs/ENTERPRISE_DEPLOYMENT_RUNBOOK.md`、`DEPLOYMENT.md`、`DEPLOYMENT_ZH.md`、`tasks.md`、`progress.md`。
- 风险和回滚：文档错误可能误导生产发布；通过只读检查现有部署脚本和环境变量来源，避免直接复用旧固定服务器脚本；回滚可删除 runbook 并恢复 deployment 文档入口和 `.env.example` 企业变量段。
- 验证计划：运行 `git diff --check`、`bun run check:layers`，并检索 runbook 中 `SUPAUTH_JWKS_URL`、`ENTERPRISE_AUTH_MODE`、`smoke:enterprise:runtime`、`/api/enterprise/health`、回滚等关键项。
- Delegation Decision：risk=low，纯文档/模板任务，不执行生产操作、不改产品代码；`safe_skip_reason=纯文档与配置模板补齐，且生产部署被明确排除，按规则跳过子代理和生产执行`。
- 结果：`docs/ENTERPRISE_DEPLOYMENT_RUNBOOK.md` 已新增；`.env.example` 已补企业变量模板；`DEPLOYMENT.md` / `DEPLOYMENT_ZH.md` 已加入三层架构和 runbook 入口。验证：`git diff --check` 通过；`bun run check:layers` 通过；关键项检索通过。orchestrator verdict=`PASS`。

### PX-001 Task Contract: 引入 @postgresx/noredis 发布版旁路验证

- 目标：在 `@postgresx/noredis` 已发布到 npm 后，为 Elygate gateway 建立可编译、可测试、可回滚的旁路 bridge，验证 Bun SQL adapter、namespace/tablePrefix/L1 cache 默认配置与发布包 import 边界；为后续真实数据库 benchmark 和渐进替换提供入口。
- 非目标：不替换现有鉴权、限流、计费、pg-boss、semantic cache 或 request hot path；不在启动时创建 postgresx 表；不部署生产；不删除现有 `@elygate/pg-listen` / `packages/pgredis` 本地实现。
- 验收标准：`apps/gateway` 显式依赖 `@postgresx/noredis` 发布版；新增 `postgresxInfra` 工厂与 smoke test；根级 `bun run check` 包含 postgresx smoke；文档说明当前为旁路 pilot 和后续 benchmark gate；不引入 Redis runtime 依赖。
- 相关 skill 和代码规范：`agent-team-delegation-gate`、`database-profile-selector`、`postgres-database`、`typescript`；遵循现有 Bun/Elysia/TypeScript 严格模式、PostgreSQL-only 方向和项目中文协作规范。
- database_profile：target=postgres；decision_source=user+detected；evidence=`@postgresx/noredis@0.5.2` npm latest、现有 `packages/db` Bun.SQL、Redis-free progress；data_scope=multi-user；consistency=transactional for future DB path；access_pattern=server-only/direct-tcp；migration_strategy=本任务无 schema migration，后续仅在 feature flag/benchmark 通过后显式 `ensureSchema`；backup_restore=无数据写入，无备份变更；verification=`bun --cwd apps/gateway test:postgresx`、`bun --cwd apps/gateway check`、`bun run check`。
- 影响范围：`apps/gateway/package.json`、`apps/gateway/src/services/postgresxInfra.ts`、`apps/gateway/src/services/postgresxInfra.test.ts`、`apps/gateway/README.md`、根级 `package.json`、`bun.lock`、`tasks.md`、`progress.md`。
- 风险和回滚：依赖包升级可能影响 install/类型解析；旁路 bridge 默认不被生产请求调用，回滚可删除依赖、测试和 bridge 文件并恢复 scripts；不触碰已有请求链路。
- 验证计划：先确认 npm latest；安装依赖并更新 lockfile；运行 postgresx smoke、gateway typecheck、根级 check；尝试独立 verifier，若子代理 runtime 继续超时则记录 interruption_recovery 和本地确定性验证。
- Delegation Decision：risk=low，主要是依赖接入和单服务新增文件；主进程直接实现以避免与 EAG-001 大范围改动并行冲突，完成前派发窄范围 verifier。若 verifier runtime ETIMEDOUT，记录 `interruption_recovery` 并以 `bun run check` 作为确定性证据。
- interruption_recovery：resume_state=blocked；last_stable_artifact=`bun --cwd apps/gateway test:postgresx`、`bun --cwd apps/gateway check`、`bun run check` 均通过；dangling_subagents=`.mailbox/PX-001-verifier-result.md` Codex runtime ETIMEDOUT、`.mailbox/PX-001-verifier-claude.md` Claude adapter 1m context error；recovery_owner=orchestrator；recovery_action=若需要完成 agent-team gate，待 subagent runtime 恢复后重跑窄 verifier，本地代码不再继续扩大范围。

### EAG-001 Task Contract: 实施三层 monorepo 企业级 AI 网关架构

- 目标：将 Elygate 明确实施为三层 monorepo 产品：基础网关层、通用面板层、企业级层。基础网关保持平台无关，面板可独立用于单机/私有化/轻量团队，企业级层基于 SupaCloud + SupAuth + svadmin 提供 IAM、实例生命周期、企业控制台、审计、企业策略和租户隔离。最终要求所有页面可访问、功能正常并达到生产级标准。
- 非目标：不复刻 new-api 或 LiteLLM 的整体产品形态；不让 SupaCloud/SupAuth 侵入基础网关热路径；不保留旧本地企业管理兼容约束；不在未核验服务、域名、回滚物的情况下直接改生产环境。
- 验收标准：monorepo 中有清晰的基础网关、面板、企业级包/应用边界；基础网关不依赖企业层包；企业层提供稳定 claims/scopes/events/contracts；gateway 能识别企业运行模式并验证 SupAuth-style claims/service token/gateway key 边界；panel/enterprise console 路由和页面可访问；旧本地登录/组织/成员能力被替换或隔离；全库类型检查、构建和针对页面的运行时 smoke 通过；关键风险、回滚方案和验证证据记录在 progress.md。
- 相关 skill 和代码规范：`agent-team-delegation-gate`、`typescript`、`elysiajs`、`svelte-core-bestpractices`、`svelte-code-writer`、`supacloud-platform`、`svadmin-admin-ui`；遵循 Bun、Elysia、Drizzle、Svelte 5 runes、svadmin provider 和项目中文交流规范。
- stack_profile：existing-monorepo；runtime=Bun + Elysia gateway；frontend=Svelte 5 admin/portal；database=PostgreSQL；decision_source=user+detected package manifests；evidence=`apps/gateway`、`apps/admin`、`apps/portal`、`packages/db`、`packages/svadmin-*`。
- deployment_profile：target=supacloud for enterprise layer；runtime_kind=fullstack/backend；target_region=private-infra；domain_dns_owner=supacloud-caddy 或 existing；stateful_services=postgres/auth/secrets/app lifecycle；verification=local build/check first, production deploy only after explicit deploy step and rollback artifact.
- admin_profile：framework=svadmin；backend_provider=elysia + enterprise adapter；auth_required=true；rbac_required=true；audit_required=true；resources=gateway instances, provider channels, model routes, gateway API keys, budgets, usage, logs, memory, audit events。
- 影响范围：`package.json` workspaces；`apps/gateway/**`；`apps/admin/**`；可能新增 `apps/enterprise-console/**`；`packages/db/**`；新增 `packages/enterprise-contracts`、`packages/enterprise-authz`、`packages/enterprise-adapter`；项目 README/部署文档/progress。
- 风险和回滚：高风险跨层改造，可能破坏 auth、admin 页面和 API key 请求热路径；按阶段实施，每阶段保持可编译；基础网关与企业层单向依赖；回滚可移除企业包并恢复 gateway 旧 auth 入口，生产部署前保留旧 artifact。
- 验证计划：基线检查 `bun run check`；每阶段运行相关包 `bun run check`/`bun run build`；Svelte 页面变更用 `npx @sveltejs/mcp svelte-autofixer`；最终启动本地服务并用浏览器/HTTP smoke 验证 gateway、panel、enterprise console 页面和核心 API。
- Delegation Decision：risk=high，多文件、多 subsystem、架构/API/auth/admin 变更；采用 explorer -> orchestrator implementation -> verifier。Explorer 只读梳理现状和边界；主进程串行实施，避免并行写入重叠文件；Verifier 独立检查依赖方向、页面/API 可访问性、类型检查和遗漏风险。
- interruption_recovery：Explorer mailbox=`.mailbox/EAG-001-explorer-result.md`，Verifier mailbox=`.mailbox/EAG-001-verifier-result.md`；二者均为 `status: error`，原因是 codex runtime `spawnSync codex ETIMEDOUT`，未产生可用完成证据。后续尝试显式 `--runtime claude` 的窄 Explorer，mailbox=`.mailbox/EAG-001-explorer-runtime-recovery.md`，失败原因是 Claude adapter API 400：`1m 上下文已经全量可用，请启用 1m 上下文后重试`。2026-06-21 再次尝试极窄 Explorer，mailbox=`.mailbox/EAG-001-narrow-explorer.md`，仍因 codex runtime `ETIMEDOUT` 失败；随后尝试极窄 Verifier，mailbox=`.mailbox/EAG-001-router-verifier.md`，仍因 codex runtime `ETIMEDOUT` 失败。2026-06-21 增量 Verifier，mailbox=`.mailbox/EAG-001-incremental-verifier.md`，仍因 codex runtime `ETIMEDOUT` 失败。resume_state=resumable；last_stable_artifact=本地确定性验证输出（`bun run check`、`bun run build`、`bun --cwd apps/gateway test:enterprise`、enterprise-console Playwright smoke、enterpriseRouter Elysia handle smoke）；dangling_subagents=explorer, verifier；recovery_owner=orchestrator；recovery_action=继续以本地确定性证据推进，但当前阶段不得声明全目标完成。
- 2026-06-21 阶段记录：新增 Enterprise 控制面投影表 `enterprise_gateway_instances`、`enterprise_identity_policies`、`enterprise_budgets`、`enterprise_audit_events` 及 migration/init.sql；新增 `/api/enterprise/overview`、`/gateway-instances`、`/identity-and-policy`、`/identity-policies`、`/usage-and-budget`、`/budgets`、`/audit-events`，并让 `/install` 持久化实例、`/events` 应用平台事件投影；enterprise-console 页面改为读取真实控制面 API 形状并渲染实例、claims/策略、用量/预算和审计数据。验证：`bun run check` 通过，`bun run build` 通过，Svelte autofixer 覆盖 5 个企业页面无 issues，静态 `/enterprise/` + manifest/me/sk 边界 smoke 通过，Playwright 生产预览在 mock API 下验证 5 个 hash 页面直达且 console errors=0。本地 Postgres 迁移 smoke 未完成，原因是当前 `DATABASE_URL` 指向的本地连接在 `CREATE SCHEMA IF NOT EXISTS "drizzle"` 时关闭，需后续修复本地 DB 连接或在可用测试库执行迁移/CRUD smoke。
- 2026-06-21 阶段补充：将 Enterprise router 改为 `createEnterpriseRouter(controlPlane)` factory，生产默认使用 `postgresEnterpriseControlPlane`，测试可注入内存 control plane；新增 `apps/gateway/src/enterprise/router.test.ts` 覆盖 SupAuth claims-scoped install、gateway instance update/list、identity policy create/list、budget create/list、platform event、audit events、`sk-*` 控制面拒绝、scope 不足拒绝和 install tenant scope mismatch；`bun --cwd apps/gateway test:enterprise` 纳入根级 `bun run check`。删除未挂载的旧本地企业 admin 路由 `apps/gateway/src/routes/admin/enterprise.ts`、`apps/gateway/src/routes/admin/enterpriseTeams.ts`，避免 Panel 重新暴露旧组织/团队/企业 token 模型。验证：`bun run check` 通过且包含 3 个企业路由测试/24 个断言；`bun run build` 通过；静态 `/enterprise/`、manifest、SupAuth `/me`、`sk-*` boundary smoke 通过。本地真实 Postgres 仍不可用：脱敏探测显示 `DATABASE_URL` 指向 `localhost:5432/postgres`，Bun.SQL `select 1` 返回 `ERR_POSTGRES_CONNECTION_CLOSED`，因此真实迁移/CRUD smoke 仍待可用测试库。
- 2026-06-21 阶段补充二：企业控制台从只读 API 视图推进到可操作控制面：`gateway-instances` 页面支持刷新和实例状态更新；`identity-and-policy` 页面支持创建企业策略；`usage-and-budget` 页面支持创建预算以及预算启停；`resources.ts` 补齐 gateway instances、identity policies、budgets、audit events 的真实 svadmin 字段定义。新增 `apps/gateway/src/enterprise/dbSmoke.ts` 与根级 `bun run smoke:enterprise:db`，用于在可用 Postgres 上验证 migration、SupaCloud install projection、实例更新、策略创建、预算创建、platform event 和 audit reads；脚本默认清理 smoke 数据并脱敏输出 DB 目标。验证：`bun --cwd apps/enterprise-console check` 通过，3 个改动 Svelte 页面 autofixer 无 issues，`bun --cwd apps/gateway check` 通过，`bun --cwd apps/gateway test:enterprise` 通过，`bun run check:layers` 通过，`bun run check` 通过，`bun run build` 通过，静态 `/enterprise/` + manifest/me/sk 边界 smoke 通过，Playwright mock API 验证 5 个企业 hash 页面无错误文本且 console errors=0，`git diff --check` 通过。限制：本机无 `docker`/`psql`，根级 `.env` 的 `DATABASE_URL` 指向 `localhost:5432/postgres`，`bun run smoke:enterprise:db` 已进入 migration 但在 `CREATE SCHEMA IF NOT EXISTS "drizzle"` 失败，真实 Postgres CRUD smoke 仍待可用测试库；增量 Verifier `.mailbox/EAG-001-incremental-verifier.md` 继续因 codex runtime `ETIMEDOUT` 失败。
- 2026-06-21 阶段补充三：保持 Elygate 三层 monorepo 隔离不变，在企业层补齐网关资源治理只读视图。gateway 企业控制面新增 `/provider-channels`、`/model-routes`、`/gateway-api-keys`、`/request-logs`、`/agent-memories`，分别受 `ai.channel.manage`、`ai.gateway.read`、`ai.key.manage`、`ai.usage.read`、`ai.memory.manage` 保护；响应统一带 `scope_kind=gateway_instance` 的企业视图 scope，避免把当前 core 表误声明为 SupAuth tenant 强隔离；渠道/API Key 只返回 mask 后凭据。enterprise-console 新增 `gateway-resources` 页面，使用 svadmin 菜单接入并提供渠道、路由、Key、日志、Memory 分段表格；`resources.ts` 同步补齐上述资源只读字段定义。验证：`bun --cwd apps/gateway check` 通过；`bun --cwd apps/gateway test:enterprise` 通过（4 tests / 37 expects，覆盖新资源路由、mask、scope、scope 不足拒绝）；`bun --cwd apps/enterprise-console check` 通过；`npx @sveltejs/mcp svelte-autofixer` 覆盖 `GatewayResources.svelte`、`App.svelte`、`EnterpriseAutoTable.svelte` 无 issues；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过；Chrome headless + 临时 mock `/api/enterprise/*` 渲染 `/enterprise/#/gateway-resources`，DOM 出现 `网关资源`、tenant scope、资源计数、`OpenAI Primary`、`gpt-4.1`。限制：`bun run smoke:enterprise:db` 仍因本机 `localhost:5432/postgres` 在 `CREATE SCHEMA IF NOT EXISTS "drizzle"` 失败，真实 Postgres migration/CRUD smoke 未通过；窄 verifier `.mailbox/EAG-001-resource-verifier.md` 运行成功但裁决 `PARTIAL`，原因是子代理隔离环境缺少 `tsc`/`svelte-check`/workspace 包解析，无法复现主线程验证命令。EAG-001 仍保持 running/PARTIAL。
- 2026-06-21 阶段补充四：补齐 SupaCloud App 生命周期的 dedicated uninstall 回调。`@elygate/enterprise-adapter` 新增 `normalizeUninstallRequest` 与 `toAppUninstalledEvent`；gateway enterprise control plane 新增 `uninstallEnterpriseGateway`，将实例投影幂等标记为 `deleted` 并写 `gateway_instance.uninstall` 审计；router 暴露 `POST /api/enterprise/uninstall`，使用 `ai.gateway.admin`，并强化 lifecycle payload 的 `app_id` / `app_instance_id` 与 SupAuth claims 边界，避免其他 app 或其他实例的回调污染 Elygate 投影。`dbSmoke.ts` 将 uninstall 纳入真实 Postgres smoke 流程；enterprise-console 的 `gateway-instances` 页面显示 uninstall callback；README/gateway README 同步生命周期和资源 API 清单。验证：`bun --cwd packages/enterprise-adapter check` 通过；`bun --cwd apps/gateway check` 通过；`bun --cwd apps/gateway test:enterprise` 通过（6 tests / 47 expects，覆盖 uninstall 幂等、其他 app/instance 拒绝）；`npx @sveltejs/mcp svelte-autofixer apps/enterprise-console/src/pages/GatewayInstances.svelte --svelte-version 5` 无 issues；`bun --cwd apps/enterprise-console check` 通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过；Chrome headless + mock API 渲染 `/enterprise/#/gateway-instances`，DOM 出现 `卸载回调` 与 `/api/enterprise/uninstall`。限制：`bun run smoke:enterprise:db` 仍因本机 `localhost:5432/postgres` 在 `CREATE SCHEMA IF NOT EXISTS "drizzle"` 失败，真实 Postgres lifecycle smoke 未通过；窄 verifier `.mailbox/EAG-001-uninstall-verifier.md` 运行成功但裁决 `PARTIAL`，原因仍是子代理隔离环境缺 `tsc`/`svelte-check` 且无法解析 workspace 包。EAG-001 仍保持 running/PARTIAL。
- 2026-06-21 阶段补充五：补齐 Cloudflare 风格身份归因 + 预算治理增量。`@elygate/enterprise-contracts` manifest 新增 `usage_attribution` 企业资源；gateway enterprise control plane 新增 `listUsageAttribution`，从 core `logs` 按模型、用户、数据面 API Key、渠道、外部用户、外部 Workspace、外部 Feature 聚合 7 日请求、额度消耗、Prompt/Completion/Cached tokens、错误数、平均耗时和最近访问，并返回 `scope_kind=gateway_instance` 与 `scope_boundary=gateway_instance_projection`，避免把当前 core logs 误声明为 SupAuth tenant 强隔离；router 暴露 `GET /api/enterprise/usage-attribution`，使用 `ai.usage.read`，`/usage-and-budget` 复用同一 attribution totals。enterprise-console `UsageAndBudget.svelte` 新增用量归因 tabs 和表格，预算创建/启停继续保留在同一企业页面；README/gateway README 同步 API 清单。验证：`bun --cwd packages/enterprise-contracts check` 通过；`bun --cwd apps/gateway check` 通过；`bun --cwd apps/gateway test:enterprise` 通过（6 tests / 53 expects，覆盖 usage attribution route、scope、workspace 样本和 scope 不足拒绝）；`npx @sveltejs/mcp svelte-autofixer apps/enterprise-console/src/pages/UsageAndBudget.svelte --svelte-version 5` 无 issues；`bun --cwd apps/enterprise-console check` 通过；`bun run check:layers` 通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过；Playwright CLI + 临时 mock API 渲染 `/enterprise/#/usage-and-budget`，DOM 出现 `用量归因`、`gpt-4.1`，切换 Workspace tab 后出现 `workspace_demo`，浏览器 console 无错误且 `/api/enterprise/usage-and-budget` 为 200。限制：`bun run smoke:enterprise:db` 仍因本机 `localhost:5432/postgres` 在 `CREATE SCHEMA IF NOT EXISTS "drizzle"` 失败；窄 verifier `.mailbox/EAG-001-usage-attribution-verifier.md` 裁决 `PARTIAL`，原因是子代理隔离环境缺 `tsc`/`svelte-check`/workspace 包解析且无法联网拉取 `@sveltejs/mcp`。EAG-001 继续保持 running/PARTIAL。
- 2026-06-21 阶段补充六：补齐 Azure/AWS 风格企业策略决策面。`@elygate/enterprise-contracts` 新增 `policy_evaluations` 资源和 `EnterprisePolicyEvaluation*` 类型；`@elygate/enterprise-authz` 新增纯函数 `evaluateEnterprisePolicies`，支持 active-only、target 匹配、模型/action/resource/scope/role/api_key/channel/external user/workspace/feature 规则匹配，并采用 deny-overrides；新增包级单测并纳入根级 `bun run check`。gateway enterprise control plane 新增 `evaluateEnterprisePolicy`，读取当前 tenant/org/app_instance 的策略投影、执行决策并写 `identity_policy.evaluate` 审计；router 暴露 `POST /api/enterprise/policy-evaluations`，使用 `ai.policy.manage`。enterprise-console `IdentityAndPolicy.svelte` 新增“策略评估”表单和命中策略结果表，策略 target 选项扩展到 tenant/app_instance/api_key/model/channel/external_user/external_workspace/feature；`dbSmoke.ts` 覆盖 allow + deny-overrides 评估流程。验证：`bun --cwd packages/enterprise-authz check` 通过；`bun --cwd packages/enterprise-authz test` 通过（3 tests / 9 expects）；`bun --cwd apps/gateway check` 通过；`bun --cwd apps/gateway test:enterprise` 通过（6 tests / 59 expects，覆盖 policy evaluation deny 与 scope 不足拒绝）；`npx @sveltejs/mcp svelte-autofixer apps/enterprise-console/src/pages/IdentityAndPolicy.svelte --svelte-version 5` 无 issues；`bun --cwd apps/enterprise-console check` 通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过；Playwright CLI + 临时 mock API 渲染 `/enterprise/#/identity-and-policy`，出现 `策略评估`，输入 `workspace_blocked` 点击评估后出现 `deny`、`Denied by enterprise policy 2` 和两条命中策略，浏览器 console 无错误且 `/api/enterprise/policy-evaluations` 为 200。限制：`bun run smoke:enterprise:db` 仍因本机 `localhost:5432/postgres` 在 `CREATE SCHEMA IF NOT EXISTS "drizzle"` 失败，真实 Postgres policy evaluation smoke 仍待可用测试库；EAG-001 继续保持 running/PARTIAL。
- 2026-06-21 阶段补充七：补齐 Cloudflare/Azure 风格预算执行决策面。`@elygate/enterprise-contracts` 新增 `budget_evaluations` 资源和 `EnterpriseBudgetEvaluation*` 类型；`@elygate/enterprise-authz` 新增纯函数 `evaluateEnterpriseBudgets`，支持 active-only、org/project/user/service_account/api_key/channel/external user/workspace/feature subject 匹配，按 requested/projected quota 计算 `allow|warn|deny`，超过 alert threshold 返回 warn，超过 limit 返回 deny，并新增包级单测纳入根级 check。gateway enterprise control plane 新增 `evaluateEnterpriseBudget`，读取当前 tenant/org/app_instance 的预算投影、执行预算决策并写 `budget.evaluate` 审计；router 暴露 `POST /api/enterprise/budget-evaluations`，使用 `ai.policy.manage`。enterprise-console `UsageAndBudget.svelte` 新增“预算评估”表单、决策卡片和命中预算表，budgets subject 选项扩展到 api_key/external_workspace/feature；`dbSmoke.ts` 覆盖预算 warn + deny 评估流程。验证：补跑阶段六窄 verifier `.mailbox/EAG-001-policy-evaluation-verifier.md` 裁决 PARTIAL（隔离环境缺 `tsc`/`svelte-check`/workspace 包解析，但 `enterprise-authz` test 与 layer check 通过）；本轮主线程 `bun --cwd packages/enterprise-contracts check` 通过；`bun --cwd packages/enterprise-authz check` 通过；`bun --cwd packages/enterprise-authz test` 通过（6 tests / 19 expects）；`bun --cwd apps/gateway check` 通过；`bun --cwd apps/gateway test:enterprise` 通过（6 tests / 67 expects，覆盖 budget evaluation warn/deny 与 scope 不足拒绝）；`npx @sveltejs/mcp svelte-autofixer apps/enterprise-console/src/pages/UsageAndBudget.svelte --svelte-version 5` 无 issues；`bun --cwd apps/enterprise-console check` 通过；`bun run check:layers` 通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过；Playwright CLI + 临时 mock API 渲染 `/enterprise/#/usage-and-budget`，点击“评估预算”后页面出现 `warn`、`Budget warning threshold reached for 1`、预计 `850`，浏览器 console error 为 0，`POST /api/enterprise/budget-evaluations` 为 200。限制：`bun run smoke:enterprise:db` 仍因本机 `localhost:5432/postgres` 在 `CREATE SCHEMA IF NOT EXISTS "drizzle"` 失败，真实 Postgres budget evaluation smoke 仍待可用测试库；EAG-001 继续保持 running/PARTIAL。
- 2026-06-21 阶段补充八：补齐 Higress/Envoy 风格控制面/数据面边界的企业 runtime guard。基础网关层新增 `apps/gateway/src/services/runtimeGovernance.ts`，`dispatcher` 只调用可选治理 hook，不直接 import 企业包；企业层新增 `apps/gateway/src/enterprise/runtimeGuard.ts` 并在 gateway 启动时注册实现，只读 enterprise 投影表和 stable contracts/authz，数据面 `sk-*` 请求仍只进入 `/v1`，但在 upstream/cache 前可执行实例 active 校验、policy deny、budget deny，budget warn 只写日志和企业审计。新增 `runtimeGuard.test.ts` 覆盖企业未配置 no-op、实例 suspended deny、policy deny-overrides、budget warn、budget deny；`scripts/check-layer-boundaries.ts` 只把新的企业适配文件加入 allowlist，继续禁止基础 gateway 其他路径和通用 panel 直接依赖企业包。验证：`bun --cwd apps/gateway test:enterprise` 通过（11 tests / 102 expects）；`bun --cwd apps/gateway check` 通过；`bun --cwd packages/enterprise-contracts check` 通过；`bun --cwd packages/enterprise-authz check` 通过；`bun --cwd packages/enterprise-authz test` 通过（6 tests / 19 expects）；`bun run check:layers` 通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过。独立 verifier `.mailbox/EAG-001-runtime-guard-verifier.md` 裁决 PARTIAL，原因是子代理隔离环境在 `bun --cwd apps/gateway test:enterprise` 前置阶段无法解析 workspace 包 `@elygate/enterprise-contracts`，后续 check/layer 命令未执行；主线程验证已提供确定性证据。限制：`bun run smoke:enterprise:db` 仍因本机 `localhost:5432/postgres` 在 `CREATE SCHEMA IF NOT EXISTS "drizzle"` 失败，真实 Postgres runtime guard smoke 仍待可用测试库；EAG-001 继续保持 running/PARTIAL。
- 2026-06-21 阶段补充九：补齐 Cloudflare 风格预算真实消耗闭环。基础网关层 `runtimeGovernance` hook 现在返回可选 usage session；`dispatcher` 在 JSON、SSE/text、binary 和 streaming 成功计费路径调用 `recordRuntimeGovernanceUsage`，基础 dispatcher 仍不直接 import 企业包。企业层 `runtimeGuard` 根据预算评估命中的 active budgets 创建 usage session，成功请求按实际 `actualQuota` 回写 `enterprise_budgets.used_quota`；warn/deny 作为治理事件记录，成功请求不向审计表写高频事件，继续以 core request logs 作为 usage ledger。新增单测覆盖 usage session 对多个匹配预算记录实际 quota，以及 deny 不产生 usage session。验证：`bun --cwd apps/gateway test:enterprise` 通过（12 tests / 113 expects）；`bun --cwd apps/gateway check` 通过；`bun run check:layers` 通过；`bun --cwd packages/enterprise-authz check && bun --cwd packages/enterprise-authz test && bun --cwd packages/enterprise-contracts check` 通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过。独立 verifier `.mailbox/EAG-001-budget-consumption-verifier.md` 裁决 FAIL，原因是子代理隔离环境无法解析 workspace 包 `@elygate/enterprise-contracts`，在第一条 `bun --cwd apps/gateway test:enterprise` 即失败，后续 check/layer/diff 命令未执行；主线程验证已提供确定性证据。限制：`bun run smoke:enterprise:db` 仍因本机 `localhost:5432/postgres` 在 `CREATE SCHEMA IF NOT EXISTS "drizzle"` 失败，真实 Postgres budget consumption smoke 仍待可用测试库；EAG-001 继续保持 running/PARTIAL。

- 2026-06-21 阶段补充十：补齐企业预算周期 rollover/reset。`@elygate/enterprise-authz` 新增 UTC 周期锚点、周期归一化和 overdue reset 推进纯函数，默认未知 period 回落到 monthly，支持 hourly/daily/weekly/monthly/quarterly/yearly/never；gateway 企业层新增 `apps/gateway/src/enterprise/budgetRollover.ts`，只读写企业投影表和审计表，在控制面 `listBudgets` / `evaluateEnterpriseBudget`、runtime guard 投影读取、成功计费回写前懒执行到期 active budgets 归零并推进下一次 `reset_at`，写入 `budget.rollover` 审计；`createBudget` / `updateBudget` 自动为 period 派生下一次 reset 时间；`dbSmoke.ts` 覆盖过期预算 reset 后再参与预算评估；README/gateway README 和 `scripts/check-layer-boundaries.ts` 同步。验证：`bun --cwd packages/enterprise-authz test` 通过（7 tests / 23 expects）；`bun --cwd packages/enterprise-authz check` 通过；`bun --cwd apps/gateway test:enterprise` 通过（12 tests / 113 expects）；`bun --cwd apps/gateway check` 通过；`bun run check:layers` 通过；`bun --cwd packages/enterprise-contracts check` 通过；`bun --cwd packages/enterprise-adapter check` 通过；`git diff --check` 通过；`bun run check` 通过；`bun run build` 通过。子代理 Explorer `.mailbox/EAG-001-budget-rollover-explorer.md` 因 codex runtime `ETIMEDOUT` 无可用探索结论；独立 Verifier `.mailbox/EAG-001-budget-rollover-verifier.md` 裁决 `PARTIAL`，原因是隔离环境缺 `tsc` 导致 check 链在 `packages/enterprise-authz check` 短路，但它确认 `enterprise-authz` test 通过。限制：`bun run smoke:enterprise:db` 仍因本机 `localhost:5432/postgres` 在 `CREATE SCHEMA IF NOT EXISTS "drizzle"` 失败，真实 Postgres budget rollover smoke 仍待可用测试库；EAG-001 继续保持 running/PARTIAL。

- 2026-06-21 阶段补充十一：解除真实 PostgreSQL smoke 阻塞并增强 fresh DB 可重复验证。`packages/db/src/init.sql` 将 budget alert 唯一索引从 `DATE(created_at)` 改为 UTC date 表达式，修复 PostgreSQL 18 下 timestamptz date cast 非 IMMUTABLE 导致空库 bootstrap 失败；`apps/gateway/src/enterprise/dbSmoke.ts` 在迁移前检测 `public.users`，空库自动执行 `packages/db/src/init.sql` 后再跑 Drizzle migrator；`apps/gateway/src/enterprise/controlPlane.ts` 修复 `queryRecord()` 返回原始 `undefined` 的 bug，避免无 query 的 audit list/export 在真实 DB smoke 中触发 `query[key]`。验证：安装本地 smoke 依赖 `postgresql@18`、`pgvector`、`pg_cron` 后启动临时 clean PostgreSQL 18（非后台服务，已停止并删除临时数据目录），原样执行 `bun run smoke:enterprise:db` 通过，输出 `ok instances=1 audits=14`；`bun --cwd apps/gateway test:enterprise` 通过（12 tests / 123 expects）；`bun --cwd apps/gateway check` 通过；`bun run check:layers` 通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过。独立 verifier `.mailbox/EAG-001-db-smoke-verifier.md` 裁决 `PARTIAL`，原因仍是隔离环境无法解析 workspace 包 `@elygate/enterprise-contracts`，但主线程同一命令链已通过。EAG-001 仍保持 running/PARTIAL，后续需继续补齐最终整体页面/生产运行验收或修复隔离 verifier 环境后再宣告完成。

- 2026-06-21 阶段补充十二：补齐最终整体运行和页面验收，同时保持 Basic Gateway / Panel / Enterprise 三层 monorepo 隔离。修复基础网关 `authPlugin` 的 Elysia lifecycle scope，将数据面鉴权从 `global` 收敛为 `scoped`，并让 `adminGuard` 显式 `.as('scoped')`，避免 `/v1`/admin 鉴权污染企业控制面 `/api/enterprise/health`；新增 `apps/gateway/src/utils/paths.ts` 并改造 static middleware / portal fallback，使 gateway 从 repo root 或 `apps/gateway` cwd 启动都能正确服务 admin、enterprise-console、portal 产物，同时修复 SPA fallback `index.html` Content-Type 被当作下载的问题；为 `channels.test_errors` 补齐 `init.sql` 和幂等 migration，修复 fresh DB schema 与 Drizzle schema 漂移；抽出 `scripts/enterprise-smoke-postgres.ts`，让 `smoke:enterprise:db` 和 `smoke:enterprise:runtime` 都使用临时 clean PostgreSQL 18，不再依赖 `.env` 的常驻本地库。验证：`bun --cwd apps/gateway check`、`bun --cwd apps/gateway test:enterprise`（12 tests / 123 expects）、`bun run check:layers`、`bun run smoke:enterprise:db`、`bun run smoke:enterprise:runtime`、`bun run check`、`bun run build`、`git diff --check` 均通过；runtime smoke 覆盖 clean DB bootstrap、真实 gateway 启动、install/policy/budget/audit API，以及 `/enterprise/#/enterprise-overview`、`gateway-instances`、`gateway-resources`、`identity-and-policy`、`usage-and-budget`、`audit-events` 六个企业页面。子代理 Explorer `.mailbox/EAG-001-runtime-health-explorer.md` 因 codex runtime `ETIMEDOUT` 无可用结论；独立 Verifier `.mailbox/EAG-001-runtime-final-verifier.md` 裁决 `PARTIAL`，隔离环境无法写 tempdir/materialize node_modules，不能执行 bun/smoke/build，但静态确认本轮边界修复、三层隔离和 `git diff --check`。按 Completion Gate，EAG-001 暂不标记 done，等待可执行 verifier 环境或人工裁决。

- 2026-06-21 阶段补充十三：补强 enterprise smoke 并行运行能力，使最终验收脚本更接近 CI/生产级可重复执行。`scripts/enterprise-smoke-postgres.ts` 新增 `findFreePort()` 和 `databaseUrlForPort()`，`startPostgres()` 默认使用 OS 分配空闲端口并返回实际 `port` / `databaseUrl`，仍支持 `ENTERPRISE_SMOKE_DB_PORT` 显式覆盖；`scripts/smoke-enterprise-db.ts` 使用返回的连接串注入 `DATABASE_URL`；`scripts/smoke-enterprise-runtime.ts` 的 gateway 端口也默认自动分配，仍支持 `ENTERPRISE_SMOKE_GATEWAY_PORT` 显式覆盖，并基于实际 gateway URL 生成 SupAuth dev JWT audience、安装回调 URL 和页面访问 URL。验证：并行执行 `bun run smoke:enterprise:db` 与 `bun run smoke:enterprise:runtime` 通过，DB smoke 使用临时 PostgreSQL `60162`，runtime smoke 使用临时 PostgreSQL `60163`、gateway `60188`，六个企业页面仍全部通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过。独立 verifier `.mailbox/EAG-001-smoke-parallel-verifier.md` 裁决 `PARTIAL`，原因是隔离 sandbox 对 `node:net listen` 返回 `EPERM` 且缺 workspace deps/Playwright，无法实际执行 smoke；其静态确认动态端口设计、独立 PGDATA、独立 PG_Boss schema/app name 和三层边界未被改变。

- 2026-06-21 阶段补充十四：补强 enterprise smoke 自动端口模式的 TOCTOU retry。`scripts/enterprise-smoke-postgres.ts` 在未显式设置 `ENTERPRISE_SMOKE_DB_PORT` 时最多重试 5 次，每次重新分配端口、创建独立 PGDATA，启动失败会清理临时目录；显式 DB 端口仍 1 次 fail-fast。`scripts/smoke-enterprise-runtime.ts` 在未显式设置 `ENTERPRISE_SMOKE_GATEWAY_PORT` 时，若 gateway 启动/ready 阶段出现 `EADDRINUSE` 或 “port in use”，会 kill 子进程、重新分配 gateway 端口并重试最多 5 次；显式 gateway 端口仍 fail-fast。验证：并行执行 `bun run smoke:enterprise:db` 与 `bun run smoke:enterprise:runtime` 通过，DB smoke 使用临时 PostgreSQL `64244`，runtime smoke 使用临时 PostgreSQL `64245`、gateway `64270`，六个企业页面全部通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过。独立 verifier `.mailbox/EAG-001-smoke-retry-verifier.md` 裁决 `PARTIAL`，静态确认 retry/fail-fast/cleanup 合同，但隔离 sandbox 仍无法执行 node:net/PostgreSQL/Playwright。

- 2026-06-21 阶段补充十五：将 enterprise smoke retry 行为从运行时 smoke 经验改为可单测契约。新增 `scripts/enterprise-smoke-retry.test.ts`，覆盖 PostgreSQL 自动端口 retry 会重新分配端口、为失败 PGDATA 调用 cleanup、成功返回第二次端口；显式 `ENTERPRISE_SMOKE_DB_PORT` 不调用端口分配且只尝试一次；gateway 自动端口只对 `EADDRINUSE` / port in use 重试并清理失败子进程，显式 gateway 端口即使 retryable 也 fail-fast，非端口占用错误不重试。根级新增 `test:enterprise:smoke` 并纳入 `bun run check`，避免该契约只依赖真实 Postgres/Playwright smoke。验证：`bun run test:enterprise:smoke` 通过（5 tests / 25 expects）；`bun run check` 通过；并行执行 `bun run smoke:enterprise:db` 与 `bun run smoke:enterprise:runtime` 通过，DB smoke 使用临时 PostgreSQL `52503`，runtime smoke 使用临时 PostgreSQL `52504`、gateway `52526`，六个企业页面全部通过；`bun run build` 通过；`git diff --check` 通过。独立 verifier `.mailbox/EAG-001-smoke-retry-unit-verifier.md` 失败为环境性 `spawnSync codex ETIMEDOUT`，未产生完整裁决；interruption_recovery：resume_state=resumable，last_stable_artifact=本条主线程验证证据，dangling_subagents=`EAG-001-smoke-retry-unit-verifier`，recovery_owner=orchestrator，recovery_action=待 subagent runtime 可用后重跑同一窄 verifier。EAG-001 继续保持 running/PARTIAL。

- 2026-06-21 阶段补充十六：补齐通用 Panel 的真实 runtime 页面验收，同时继续保持 Basic Gateway / Panel / Enterprise 三层 monorepo 隔离。新增 `apps/admin/src/components/PanelSafeTable.svelte`，让 `channels`、`tokens`、`logs` 使用 Panel 层安全只读表格，避免发布版 svadmin AutoTable 在真实 gateway smoke 中触发递归栈溢出；`CustomAutoTable.svelte` 继续只拦截 Panel 自有虚拟页面和这三个资源页，旧企业控制台入口保持移除，其他资源仍走默认 svadmin；`Settings.svelte` 为 admin options 和 embedding 检查补齐 Bearer token header。`scripts/smoke-enterprise-runtime.ts` 覆盖 Panel dashboard、channels、tokens、logs、system-options 五页，并继续覆盖 Enterprise 六页。根据 verifier 建议，`scripts/check-layer-boundaries.ts` 将 `@elygate/enterprise-contracts` 纳入 `apps/admin/src` 禁止 import 清单，并仅允许企业包内部测试/适配入口引用 contracts，强化三层边界。验证：`npx @sveltejs/mcp svelte-autofixer apps/admin/src/components/PanelSafeTable.svelte --svelte-version 5`、`npx @sveltejs/mcp svelte-autofixer apps/admin/src/components/CustomAutoTable.svelte --svelte-version 5` 均无 issues；`bun --cwd apps/admin check` 通过；`bun run smoke:enterprise:runtime` 通过，临时 PostgreSQL `61535`、gateway `61536` 下 Panel 五页与 Enterprise 六页全部 page ok；`bun --cwd apps/enterprise-console check` 通过；`bun --cwd apps/gateway check` 通过；`bun run smoke:enterprise:db` 通过；`bun run check:layers` 通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过。Explorer `.mailbox/EAG-001-panel-smoke-explorer.md` 因 codex runtime `ETIMEDOUT` 失败；Verifier `.mailbox/EAG-001-panel-runtime-verifier.md` 裁决 `PASS`，确认 Panel 增量未引入企业依赖、admin package 无企业包依赖、runtime smoke 覆盖新增安全表格页面。EAG-001 仍保持 running/PARTIAL，不因本增量单独标 done。

- 2026-06-21 阶段补充十七：补齐通用 Panel 的函数级 runtime smoke，并再次确认 Elygate 仍是 Basic Gateway / Panel / Enterprise 三层 monorepo 隔离，不把企业级能力塞回通用面板或基础网关热路径。`scripts/smoke-enterprise-runtime.ts` 新增 `panelRequest()` 与 `verifyPanelApiFunctions()`，在 `loginPanelAdmin()` 获取 token 后，真实调用 `/api/admin` 覆盖 channels list/create/update/delete、tokens create/update/regenerate/delete、settings `PUT /api/admin/options` 后 `GET /options` 校验、logs list 命中 `trace_runtime_smoke`；`verifyPanelPages()` 的 response listener 对 `/api/admin` 和 `/api/auth` 的 `>=400` 均记为失败，Panel 页面也断言 `Runtime Smoke OpenAI`、`Runtime Smoke Key`、`gpt-4.1` 等种子数据。Explorer `.mailbox/EAG-001-panel-function-explorer.md` 裁决 `PARTIAL`，指出此前 Panel smoke 只有页面/GET 覆盖；Verifier `.mailbox/EAG-001-panel-function-verifier.md` 静态确认函数 smoke、调用顺序与 `check-layer-boundaries` 规则，但因隔离环境缺 Bun/Playwright/PostgreSQL 只能裁决 `PARTIAL`。主线程验证：`bun run smoke:enterprise:runtime` 通过，临时 PostgreSQL `65397`、gateway `65402` 下输出 `panel admin API functions ok`，Panel 五页与 Enterprise 六页全部通过；`bun --cwd apps/gateway check`、`bun run check`、`bun run build`、`bun run smoke:enterprise:db`、`git diff --check` 均通过。EAG-001 继续保持 running/PARTIAL，等待最终 completion audit 或用户裁决。

- 2026-06-21 阶段补充十八：继续补齐通用 Panel 的页面可访问和功能级 runtime 验收，同时不改变三层 monorepo 边界。Panel 直接 fetch 页面统一补 Bearer 身份头，覆盖 `ModelsStatus.svelte`、`ContentManagement.svelte`、`PricingEditor.svelte`、`PerformanceMonitor.svelte`、`MemoryManagement.svelte`、`Playground.svelte`，保证只注入 localStorage `auth_token` 的真实浏览器上下文也能访问后台 API；修复 Playground 模型列表从错误 `/api/v1/models` 改为真实数据面 `/v1/models`；修复倍率接口后端路径，`performance.ts` 现在提供前端实际调用的 `/api/admin/performance/ratio-config`、`PUT /api/admin/performance/ratio-config`、`/api/admin/performance/ratio-config/missing`；`models-meta` 加入 `PanelSafeTable`，避免模型管理页触发发布版 svadmin AutoTable 递归栈溢出。`scripts/smoke-enterprise-runtime.ts` 新增 `model_metadata` seed，并将 Panel 函数断言扩展到模型状态、模型元数据 create/update/delete、performance stats、ratio config update/read、content update/read、memory stats/list；Panel 页面 smoke 扩展为 dashboard、channels、tokens、models、models-meta、logs、pricing-editor、content-management、performance-monitor、memory-management、playground、feature-console、system-options 共 13 页，response listener 对 Panel 相关 `/api/*` 和 `/v1/*` 的 `>=400` 均失败。验证：改动 Svelte 页面与 `CustomAutoTable.svelte` 的 Svelte autofixer 均无 issues；`bun --cwd apps/admin check` 通过；`bun --cwd apps/gateway check` 通过；`bun run smoke:enterprise:runtime` 通过，临时 PostgreSQL `55891`、gateway `55897` 下新增 Panel 13 页与 Enterprise 6 页全部 `page ok`；`bun run smoke:enterprise:db`、`bun run check`、`bun run build`、`git diff --check` 均通过。独立 verifier `.mailbox/EAG-001-panel-expanded-verifier.md` 裁决 `PARTIAL`，原因是隔离环境在 `bun run smoke:enterprise:runtime` 首项监听 `127.0.0.1` 失败，后续 check 未执行；主线程验证链已通过。EAG-001 仍保持 running/PARTIAL，不能仅凭本增量标记完成。

- 2026-06-21 阶段补充十九：继续推进“所有页面可访问、所有功能正常”的生产级 gate，本轮补齐通用 Panel 全菜单 runtime 覆盖并修复 smoke 暴露的真实缺陷。`apps/gateway/src/routes/admin/index.ts` 将 `/api/admin/self/*` 从管理员角色 guard 中拆出，改为仅要求认证，普通用户自助信息和自助 Token API 不再被 super admin guard 误拦截；`apps/gateway/src/routes/admin/vendors.ts` 补齐 svadmin 标准 `PUT /api/admin/vendors/:id`，供应商编辑不再依赖非标准 `PUT /vendors`；`apps/gateway/src/routes/admin/data.ts` 修正备份统计接口，使用白名单表名 + Bun.SQL 查询，避免返回 `-1`；`apps/admin/src/components/CustomAutoTable.svelte` 使用 `{#key resourceName}` 强制资源切换时重建组件，并将 users、user-groups、packages、redemptions、vendors、rate-limits 纳入 Panel 安全表格，修复从 channels 切到 users 时表格仍显示渠道数据的运行时问题。`scripts/smoke-enterprise-runtime.ts` 新增 user_groups/rate_limit_rules/packages/redemptions/vendors seed，并将 Panel 函数断言扩展到 users CRUD、普通用户 self info/self tokens、user-groups CRUD、rate-limits CRUD、packages CRUD、redemptions CRUD、vendors CRUD、data backup status、data export；Panel 页面 smoke 扩展为 dashboard、channels、users、tokens、user-groups、packages、redemptions、vendors、rate-limits、models、models-meta、logs、pricing-editor、content-management、performance-monitor、memory-management、playground、feature-console、system-options 共 19 页，Enterprise 继续覆盖 6 页。验证：`npx @sveltejs/mcp svelte-autofixer apps/admin/src/components/CustomAutoTable.svelte --svelte-version 5` 无 issues；`bun --cwd apps/gateway check`、`bun --cwd apps/admin check`、`bun run smoke:enterprise:runtime` 通过（临时 PostgreSQL `61832`、gateway `61835`，Panel 19 页与 Enterprise 6 页全部 `page ok`）；`bun run smoke:enterprise:db` 通过；`bun run check:layers` 通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过。独立 Explorer `.mailbox/EAG-001-completion-gap-explorer.md` 与独立 Verifier `.mailbox/EAG-001-panel-full-menu-verifier.md` 均因 codex runtime `ETIMEDOUT` 失败，未产生可用 completion audit；EAG-001 仍保持 running/PARTIAL，不宣告全目标完成。

- 2026-06-21 阶段补充二十：补齐 Basic Gateway 数据面真实 runtime smoke，并修复 billing 明细落库缺陷，同时继续保持 Basic Gateway / Panel / Enterprise 三层 monorepo 隔离。`scripts/smoke-enterprise-runtime.ts` 新增 mock OpenAI upstream 和 `verifyBasicGatewayDataPlane()`：在 clean PostgreSQL + 真实 gateway 下用 `sk-runtime-smoke-key` 调 `/v1/models` 与 `/v1/chat/completions`，断言模型 `gpt-4.1` 可见、请求真实路由到 mock upstream、provider Authorization 为 `Bearer sk-runtime-smoke-provider`、chat 响应内容正确、core `logs` 记录 prompt/completion/cached tokens，以及 enterprise runtime budget `used_quota` 从 100 增长；失败时会输出 exact logs、最近 token logs、enterprise budgets 和 pg-boss billing job 诊断。首次 runtime smoke 诊断确认 budget 已回写、pg-boss `billing.flush` 已入队但持续 retry，根因是 `log_details(log_id, log_created_at)` 复合外键因 `logs.created_at` 数据库微秒精度与 JS Date 毫秒精度不一致而匹配失败；`apps/gateway/src/services/billing.ts` 现在在 `processBillingJobs()` 内为 `logs` 和 `log_details` 使用同一个显式 `createdAt`，避免 partitioned logs FK 精度漂移。验证：`bun --cwd apps/gateway check` 通过；`bun run check:layers` 通过；`bun run smoke:enterprise:runtime` 通过（临时 PostgreSQL `52221`、mock upstream `52226`、gateway `52227`，输出 `basic gateway data-plane ok`、`panel admin API functions ok`，Panel 19 页与 Enterprise 6 页全部 ok）；`bun run smoke:enterprise:db` 通过；`bun --cwd apps/admin check`、`bun --cwd apps/enterprise-console check`、`bun --cwd packages/enterprise-adapter check`、`bun --cwd packages/enterprise-authz check`、`bun --cwd packages/enterprise-contracts check` 均通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过。独立 verifier `.mailbox/EAG-001-basic-dataplane-verifier.md` 因 codex runtime `ETIMEDOUT` 失败，未产生可用裁决；interruption_recovery：resume_state=resumable，last_stable_artifact=本条主线程验证证据，dangling_subagents=`EAG-001-basic-dataplane-verifier`，recovery_owner=orchestrator，recovery_action=待 subagent runtime 可用后重跑窄 verifier。EAG-001 仍保持 running/PARTIAL，不宣告全目标完成。

- 2026-06-21 阶段补充二十一：补强 Basic Gateway 核心功能 runtime smoke，使基础网关层不仅证明 OpenAI-compatible proxy / provider adapter / routing / metering / logs，也证明缓存、限流和 Memory 数据面行为。`scripts/smoke-enterprise-runtime.ts` 的 clean DB seed 新增 `sk-runtime-smoke-limited-key`，并开启 `MemoryEnabled`；mock OpenAI upstream 现在记录上游 `messages`。`verifyBasicGatewayDataPlane()` 新增三项断言：response cache 使用与 `responseCache.ts` 一致的 hash 计算，第一次相同消息调用上游并落 `response_cache`，第二次同消息调用不增加 mock upstream 请求数且 cache hit billing log 以 `channel_id=-1` 落入 `logs`；token rate limit 使用 rate_limit=1 的专用 key 验证第一次 `/v1/models` 成功、第二次 HTTP 429；Memory read 请求带 `memory:'read'`，断言上游 system message 包含 seed `runtime smoke memory`。验证：`bun --cwd apps/gateway check` 通过；`bun run check:layers` 通过；`bun run smoke:enterprise:runtime` 通过（临时 PostgreSQL `56453`、mock upstream `56454`、gateway `56455`，Basic Gateway data-plane ok，Panel 19 页与 Enterprise 6 页全部 ok）；`bun run smoke:enterprise:db` 通过；`bun run check` 通过；`bun run build` 通过；`git diff --check` 通过。独立 verifier `.mailbox/EAG-001-basic-core-smoke-verifier.md` 裁决 `PASS`：确认 response cache、rate limit、Memory 注入断言与 `responseCache.ts`、`auth.ts`/`ratelimit.ts`、`chat.ts`/`memory.ts` 实现路径对应，并在 verifier 环境执行 `bun run check:layers` 通过。EAG-001 仍保持 running/PARTIAL，下一步继续完整 completion audit，不因单个阶段 PASS 就缩小目标或宣告完成。

- 2026-06-21 最终完成审计：EAG-001 已按用户要求收口为 Basic Gateway / Panel / Enterprise 三层 monorepo 架构。最终补丁将 `apps/gateway/src/index.ts` 的 enterprise runtime guard 改为仅在 `enterpriseRuntimeConfig.enabled` 时安装，基础网关默认热路径不再挂企业 no-op guard；`/api/enterprise/*` 与 `/enterprise/` 仍作为显式企业集成入口存在。需求逐条审计通过：Basic Gateway 覆盖 OpenAI-compatible proxy、provider adapter、routing、rate limit、metering、logs、response cache、Memory；Panel 保持通用 svadmin 管理面板并覆盖渠道、模型、API Key、用户、套餐、兑换码、供应商、限流、用量、日志、设置等；Enterprise 基于 SupaCloud + SupAuth + svadmin，覆盖 manifest、install/uninstall/events、SupAuth claims/scopes、tenant/org/app_instance scope、实例投影、策略、预算、用量归因、审计导出、网关资源治理和可选数据面 runtime governance。验证：`bun run check` 通过；`bun run build` 通过；`bun run smoke:enterprise:db` 通过（临时 PostgreSQL `61320`）；`bun run smoke:enterprise:runtime` 通过（临时 PostgreSQL `61419`、mock OpenAI `61420`、gateway `61421`，Basic Gateway data-plane ok，Panel 19 页 ok，Enterprise 6 页 ok）；`git diff --check` 通过；补充例外路径扫描确认 `apps/admin/src`、`apps/gateway/src/services|routes|middleware` 无企业包 import，企业包 import 仅在 `apps/gateway/src/enterprise/*` 和 `index.ts` 显式组合点。独立 verifier `.mailbox/EAG-001-final-static-verifier.md` 裁决 `PASS`；先前 `.mailbox/EAG-001-final-completion-verifier.md` 的 `FAIL` 为隔离环境 workspace 包解析失败，已由主线程同命令和后续 verifier PASS 复核。最终 orchestrator verdict=`PASS`，EAG-001 标记 `done`。

### AM-002 Task Contract: 完整实施 Elygate Memory 运营能力

- 目标：在 AM-001 MVP 基础上补齐生产可运营闭环：pg-boss 异步写入 worker、admin memory API、后台管理页面、系统设置项、统计/删除/清理能力、文档和验证。
- 非目标：不接入 TencentDB/Mem0/Zep/Qdrant 外部 provider；不默认启用 Memory；不做自动摘要模型调用链；不部署生产；不修改无关业务流程。
- 验收标准：Memory 写入走 `memory.remember` pg-boss 队列并有直接写入 fallback；管理员可查看统计、分页检索、软删除、按条件清理；系统设置可配置 Memory 开关/读写默认/注入条数/默认 scope；后台页面通过 Svelte 检查；全库 `bun run check` 通过。
- 相关 skill 和代码规范：`agent-team-automation`、`database-profile-selector`、`postgres-database`、`typescript`、`svelte-code-writer`、`svelte-core-bestpractices`；遵循 PostgreSQL-only、Bun、Svelte 5 runes、项目 admin API 风格。
- database_profile：target=postgres；decision_source=detected+AM-001裁决；evidence=`agent_memories` LOGGED 表、`pg-boss` worker 启动点、admin routes/resources；data_scope=multi-user；consistency=transactional；access_pattern=server-only/direct-tcp；migration_strategy=沿用 AM-001 新增表，无破坏性 schema 变更；backup_restore=现有 PostgreSQL WAL/备份；verification=`bun run check`。
- 影响范围：`apps/gateway/src/services/memory.ts`、`apps/gateway/src/services/jobQueue.ts`、`apps/gateway/src/routes/admin/memory.ts`、`apps/gateway/src/routes/admin/index.ts`、`apps/admin/src/pages/MemoryManagement.svelte`、`apps/admin/src/components/CustomAutoTable.svelte`、`apps/admin/src/resources.ts`、`apps/admin/src/pages/Settings.svelte`、`README.md`、`progress.md`。
- 风险和回滚：Memory 长期内容敏感，保留默认关闭、软删除、管理员清理；队列失败 fallback 到直接写入以保护功能但记录告警；回滚可关闭 `MemoryEnabled` 并保留新增表。
- 验证计划：运行 `bun --cwd apps/gateway check`、`npx @sveltejs/mcp svelte-autofixer apps/admin/src/pages/MemoryManagement.svelte --svelte-version 5`、`bun run check`。
- safe_skip_reason：用户明确要求当前会话“完整实施”；AM-001 已经由 gpt-5.3-codex 裁决 PASS，本轮由主进程串行实施并运行独立验证，避免并行写入同一批 gateway/admin 文件造成冲突。

### AM-001 Task Contract: 内置 Postgres-native Elygate Memory MVP

- 目标：在 Elygate 核心内置 MemoryProvider 接口与默认 Postgres-native provider，新增持久化 memory 表与最小 chat/responses 可选注入、异步写入链路；功能默认关闭，关闭态零副作用。
- 非目标：本次不实现 TencentDB/Mem0/Zep/Qdrant 等外部 provider；不默认启用记忆；不实启新的 pg-boss memory worker；不实现后台 UI；不改变现有 semantic_cache/response_cache 行为。
- 验收标准：`agent_memories` 为 LOGGED 普通表；记录 `user_id`、`token_id` 并使用外键级联隔离；召回路径仅在 `MemoryEnabled` 和请求/策略允许时执行；关闭态不检索、不写入、不注入；chat/responses 原有请求行为保持兼容；gateway TypeScript 检查通过。
- 相关 skill 和代码规范：`agent-team-automation`、`database-profile-selector`、`postgres-database`、`typescript`；遵循项目 PostgreSQL-only、Bun、Drizzle schema/migration、中文说明规范。
- database_profile：target=postgres；decision_source=detected+gpt-5.3-codex裁决；evidence=`DATABASE_URL`、`packages/db/src/init.sql` pgvector、`apps/gateway/package.json` pg-boss、现有 assistants/threads/vector_stores；data_scope=multi-user；consistency=transactional；access_pattern=server-only/direct-tcp；migration_strategy=新增非破坏性 Drizzle SQL migration；backup_restore=LOGGED 表进入现有 PostgreSQL 备份/WAL；verification=`bun --cwd apps/gateway check`。
- 影响范围：`packages/db/src/schema.ts`、`packages/db/src/init.sql`、`packages/db/drizzle/*add_agent_memory*/migration.sql`、`apps/gateway/src/services/memory.ts`、`apps/gateway/src/routes/chat.ts`、`apps/gateway/src/routes/responses.ts`。
- 风险和回滚：长期记忆可能包含敏感数据，因此默认关闭并按 user/token 隔离；回滚可关闭 `MemoryEnabled` 并回退新增代码，数据库表为新增非破坏性对象，可保留或后续显式 drop。
- 验证计划：先跑 gateway 基线 `bun --cwd apps/gateway check`；实施后再次运行；必要时检查 migration SQL 无 DROP/破坏性操作。
- safe_skip_reason：gpt-5.3-codex 裁决要求如不实启 pg-boss 需记录原因；本 MVP 选择 fire-and-forget 异步写入，不启动新的 pg-boss worker，因为 gateway 已有 pg-boss 依赖但 memory worker 生命周期/重试语义属于独立基础设施变更，留作 follow-up。
- 裁决证据：gpt-5.3-codex 只读裁决 Verdict=PASS；要求强制 user_id/token_id 隔离、LOGGED 表、默认关闭、外部后端插件化。

### NR-001 Task Contract: 审计 new-api/Redis 使用点并形成迁移清单

- 目标：全面检索 Elygate 中 new-api 兼容层、gateway 服务、缓存、限流、队列、Pub/Sub、锁相关实现，识别所有 Redis 或 Redis-like 依赖/语义，并按业务域归类为 cache、rateLimit、pubsub、lock、queue、stream/unsupported。
- 非目标：不修改业务行为；不引入 `@postgresx/noredis`；不删除现有实现。
- 验收标准：产出一份迁移清单，列出文件路径、当前语义、目标 `@postgresx/noredis`/`pg-boss` 能力、风险等级、建议迁移顺序；确认是否存在 Redis Streams、Lua、blocking pop、Cluster/Sentinel 等不可直接替代语义。
- 相关 skill 和代码规范：遵循项目 `AGENTS.md`；优先使用 `rg`；保持只读审计；中文输出。
- 影响范围：`apps/gateway/src/**`、`packages/db/src/**`、`package.json`、new-api 兼容路由与服务。
- 风险和回滚：只读任务，无代码回滚需求。
- 验证计划：运行 `rg` 检索 Redis/ioredis/Bull/queue/rateLimit/pubsub/lock/cache 关键词；必要时读取相关服务实现并交叉检查 package 依赖。
- 参考：`@postgresx/noredis` 已发布；目标是 Redis 功能替代，不是命令兼容。

### NR-002 Task Contract: 使用 @postgresx/noredis 迁移缓存/KV/TTL 场景

- 目标：将适合 Redis cache/KV/TTL 的业务缓存迁到 `@postgresx/noredis` 的 `PgKvCache` 或 `createPgredis().cache`，优先覆盖 token/session/model/new-api 配置等读多写少缓存。
- 非目标：不迁移队列；不迁移复杂 Redis Streams/模块能力；不宣称 Redis 命令兼容。
- 验收标准：相关缓存读写通过 `@postgresx/noredis` adapter；Bun 环境使用 `Bun.SQL` adapter；Node 环境如存在则显式安装并使用 `pg` adapter；缓存 TTL、批量读写、prefix/namespace 清理行为有单测或集成验证。
- 相关 skill 和代码规范：Elysia/gateway 既有服务风格；数据库变更需按项目迁移规范；避免新增全局 Redis 依赖。
- 影响范围：`apps/gateway/src/services/cache.ts`、`apps/gateway/src/middleware/auth.ts`、new-api 兼容相关服务、必要的 env/config。
- 风险和回滚：缓存语义变化可能影响登录、授权和配置刷新；保留 feature flag 或小范围回滚路径，必要时可切回现有 PG 查询路径。
- 验证计划：新增/更新缓存服务单测；跑 `bun run check`、相关 gateway 测试；本地用 Postgres 验证 TTL 到期和 cleanup。
- parent/source/reason：parent=NR-001；source=用户要求用 `@postgresx/noredis` 替代 new-api Redis；reason=先迁缓存收益最大、风险最低。

### NR-003 Task Contract: 迁移限流、Pub/Sub 失效通知和分布式锁

- 目标：将适合 Redis 的 API 限流、缓存失效通知、分布式互斥迁到 `@postgresx/noredis` 的 rate limiter、Postgres `LISTEN/NOTIFY`、advisory lock。
- 非目标：不迁移队列；不实现 Redis Pub/Sub pattern subscription；不实现 Redlock lease renewal 语义。
- 验收标准：限流结果字段与现有调用方兼容；Pub/Sub 仅用于轻量失效/通知且注明非 durable；关键互斥任务使用 transaction-scoped advisory lock；新增异常降级策略。
- 相关 skill 和代码规范：遵循 existing `ratelimit.ts` PG fixed-window 语义；避免破坏 `modelRateLimit`、package concurrency 等现有行为。
- 影响范围：`apps/gateway/src/services/ratelimit.ts`、`apps/gateway/src/services/modelRateLimit.ts`、缓存刷新/失效路径、billing/task 互斥点。
- 风险和回滚：限流误判会影响请求可用性；通知丢失可能造成短期缓存不一致；锁作用域错误可能导致任务重复或阻塞。保留旧逻辑 feature flag 或配置开关。
- 验证计划：单测覆盖 allowed/remaining/retryAfter、并发 hit、TTL cleanup；集成验证 `LISTEN/NOTIFY`；锁测试覆盖 busy/non-busy 与事务回滚。
- parent/source/reason：parent=NR-001；source=用户要求完全不需要 Redis；reason=限流/通知/锁是 Redis 常见基础设施能力。

### NR-004 Task Contract: 用 pg-boss/@postgresx/noredis 队列边界替代 Redis 队列场景

- 目标：梳理并迁移 Redis/Bull-like 队列场景到现有 `pg-boss` 队列边界，保持 billing flush、异步 webhook、长任务处理优先级最高；如使用 `@postgresx/noredis` 队列 wrapper，仅作为薄封装，不隐藏 `pg-boss` 语义。
- 非目标：不实现 Redis Streams；不实现 Bull API 兼容层；不改变业务 payload 合约。
- 验收标准：billing flush、webhook delivery、long task enqueue/work/retry 行为可在无 Redis 环境下运行；队列创建、重试、退避、worker 启停、错误日志保持可观测；失败时有 fallback 或明确告警。
- 相关 skill 和代码规范：遵循现有 `apps/gateway/src/services/jobQueue.ts`、`billing.ts`、`webhook.ts`、`task-service.ts` 风格；不引入 Redis 客户端。
- 影响范围：`apps/gateway/src/services/jobQueue.ts`、`billing.ts`、`webhook.ts`、`task-service.ts`、gateway startup。
- 风险和回滚：队列迁移影响计费、通知和长任务可靠性；保留直接执行/fallback 路径和环境变量开关。
- 验证计划：单测 queue wrapper；本地 Postgres 集成测试 enqueue/work/retry；启动 gateway 验证 worker 注册；失败路径日志断言。
- parent/source/reason：parent=NR-001；source=已决定优先 pg-boss；reason=队列类任务收益最大且代码改动可控。

### NR-005 Task Contract: 补齐 Redis-free 验证、配置文档和回滚路径

- 目标：确保 Elygate/new-api 兼容层可以在没有 Redis 服务、没有 Redis 客户端依赖的部署中运行，并将安装、环境变量、迁移步骤、回滚方式写入项目文档。
- 非目标：不新增业务功能；不做性能调优；不发布 npm 包。
- 验收标准：`package.json` 不含 Redis runtime 依赖；启动文档说明 Postgres-only 架构；测试覆盖 Redis-free smoke；CI 或本地验证命令清晰；回滚路径明确。
- 相关 skill 和代码规范：遵循项目部署/验证规范；所有文档中文优先，必要 API 名称保留英文。
- 影响范围：root/app package manifests、README/部署文档、env example、CI 测试脚本。
- 风险和回滚：文档与实际配置不一致会误导部署；通过 smoke test 和依赖扫描降低风险。
- 验证计划：`rg` 确认无 Redis 客户端依赖；`bun install`/`bun run check`/相关测试；无 Redis 环境启动 gateway smoke；Postgres-only 配置验证。
- parent/source/reason：parent=NR-002/NR-003/NR-004；source=用户目标“完全不需要 Redis”；reason=迁移完成必须有可验证的部署边界。
