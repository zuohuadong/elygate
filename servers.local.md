# 基础设施信息（本地文件，不提交到 Git）

> ⚠️ 此文件已被 `.gitignore` 排除，仅存在于本机。填入实际信息后，Agent 可自动读取并连接。

---

## 🟢 开发环境 (Development)

### 服务器

| 名称 | IP / 域名 | SSH 端口 | 用户 | 登录命令 | 用途 |
|------|-----------|----------|------|----------|------|
| dev-app | | 22 | | `ssh user@ip` | 应用服务 |

### 数据库

| 名称 | 类型 | 地址 | 端口 | 数据库名 | 连接串 |
|------|------|------|------|----------|--------|
| dev-pg | PostgreSQL | | 5432 | | `postgresql://user:pass@host:5432/db` |
| dev-redis | Redis | | 6379 | - | `redis://host:6379` |

### 对象存储

| 名称 | 类型 | Endpoint | Bucket | Region |
|------|------|----------|--------|--------|
| dev-s3 | S3 / MinIO | | | |

### 其他服务

| 名称 | URL | 用途 |
|------|-----|------|
| dev-mq | | 消息队列 |

---

## 🟡 测试环境 (Staging)

### 服务器

| 名称 | IP / 域名 | SSH 端口 | 用户 | 登录命令 | 用途 |
|------|-----------|----------|------|----------|------|
| stg-app | | 22 | | `ssh user@ip` | 应用服务 |

### 数据库

| 名称 | 类型 | 地址 | 端口 | 数据库名 | 连接串 |
|------|------|------|------|----------|--------|
| stg-pg | PostgreSQL | | 5432 | | |

### 对象存储

| 名称 | 类型 | Endpoint | Bucket | Region |
|------|------|----------|--------|--------|
| stg-s3 | S3 | | | |

---

## 🔴 生产环境 (Production)

### 服务器

| 名称 | IP / 域名 | SSH 端口 | 用户 | 登录命令 | 用途 |
|------|-----------|----------|------|----------|------|
| prod-app | | 22 | | `ssh user@ip` | 应用服务 |
| prod-worker | | 22 | | `ssh user@ip` | 后台任务 |

### 数据库

| 名称 | 类型 | 地址 | 端口 | 数据库名 | 连接串 |
|------|------|------|------|----------|--------|
| prod-pg | PostgreSQL | | 5432 | | |
| prod-redis | Redis | | 6379 | - | |

### 对象存储

| 名称 | 类型 | Endpoint | Bucket | Region |
|------|------|----------|--------|--------|
| prod-s3 | S3 | | | |

### 其他服务

| 名称 | URL | 用途 |
|------|-----|------|
| CDN | | 静态资源 |
| 监控 | | Grafana / Prometheus |

---

## 常用操作速查

```bash
# 快速连接
ssh dev-app      # 需在 ~/.ssh/config 中配置 Host 别名

# 数据库连接
psql "postgresql://user:pass@host:5432/db"

# 部署
ssh prod-app "cd /app && git pull && bun install && pm2 restart all"
```

---

## 备注

- 密码和 Token 建议使用 `~/.ssh/config` + 密钥登录，不要写在此文件中
- 如需存储 API Key，使用环境变量或密钥管理器
