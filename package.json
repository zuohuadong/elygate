{
  "name": "elygate",
  "version": "0.1.0",
  "description": "AI API 批发平台",
  "private": true,
  "scripts": {
    "dev": "bun --cwd apps/gateway dev & bun --cwd apps/web dev",
    "build": "bun --cwd apps/web build",
    "start": "bun --cwd apps/gateway start & bun --cwd apps/web start"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.3.0"
  }
}